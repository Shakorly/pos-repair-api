# Application
NODE_ENV=
PORT=
API_PREFIX=

# Database
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=

# JWT
JWT_SECRET=
JWT_EXPIRATION=

# Security
BCRYPT_ROUNDS=

# Business Rules
MAX_TERMINALS_PER_TECH=
MAX_PENDING_QA_REPAIRS=
```

**Why two files?**
```
.env         = Real values (secrets) → NOT committed
.env.example = Template → COMMITTED to Git